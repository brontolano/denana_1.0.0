{
  "name": "denana",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "cd frontend && npm install && npm run build && cp -r dist ../dist",
    "dev": "cd frontend && npm run dev"
  }
}